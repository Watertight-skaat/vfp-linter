This is a vscode Foxpro linter extension repository. The extension is available publicly. The linter works, but doesn't currently support all Foxpro syntax and never got to the point of implementing really nice linting rules.

Conventions:
- Be as concise as you can in your code and comments
- Comments should be on one line (modern IDEs have text wrap)