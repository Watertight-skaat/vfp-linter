4:10 warning private-all PRIVATE ALL hides every variable of the caller from this routine and everything it calls. Name the variables it needs to hide, or declare the ones this routine owns as LOCAL.
6:2 warning implicit-private 'm_total' is assigned but never declared, so FoxPro creates it as a PRIVATE: it stays visible to everything RebuildTotals calls, and a mistyped name becomes a new variable rather than an error. Declare it LOCAL to keep it here, or PRIVATE to say the visibility is deliberate.
7:2 warning implicit-private 'm_count' is assigned but never declared, so FoxPro creates it as a PRIVATE: it stays visible to everything RebuildTotals calls, and a mistyped name becomes a new variable rather than an error. Declare it LOCAL to keep it here, or PRIVATE to say the visibility is deliberate.
