* Valid FoxPro the grammar does not cover yet. These must reach UnknownStatement rather than being chopped into bogus expression statements, which is what happened before ExpressionStatement stopped accepting a bare word.
SEEK lcKey
PACK
CLOSE DATABASES
RELEASE loObj
THROW "boom"
ON ERROR DO handler
