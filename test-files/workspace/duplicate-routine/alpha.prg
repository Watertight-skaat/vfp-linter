* One of two files defining Shared. FoxPro runs whichever it finds first, which depends on the search order rather than on anything written here.
PROCEDURE Shared
LPARAMETERS tcName
? m.tcName
ENDPROC
